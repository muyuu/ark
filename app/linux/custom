# ==================================================
# Linux custom インストーラ（GUI 環境のみ）
# ==================================================
# PM に無い、または distro 横断で導入経路が異なるアプリを engine の個別インストーラ名で宣言する。
# 実体は engine/lib/linux/。GUI を持つ環境にだけ適用する（WSL / headless では実行しない）。
# ==================================================

# エディタ
zed     # IDE はホスト連携が要るので flatpak でなくネイティブ。Arch=PM / Debian・Fedora=公式 install スクリプト
