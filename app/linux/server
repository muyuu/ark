# ==================================================
# Linux サーバ向けパッケージ（distro PM・サーバのみ）
# ==================================================
# 論理パッケージ名を記載する。distro で名前が異なる場合は distro/ のマップで変換する。
#
# 表示先の無い Linux（VPS・コンテナ）にだけ適用する。開発機・デスクトップには入れない。
# ここは「ssh して作業する人」のための物だけ。ホストの構成（ファイアウォール・Docker・
# 自動更新など）は keel が持つので置かない。
# ==================================================

# 切断されても作業を続けられるように（開発機は common/Brewfile の tmux が入る）
tmux
