# ==================================================
# Linux システムパッケージ（ディストリビューション共通・CLI / システム層）
# ==================================================
# 論理パッケージ名を記載する。実際のパッケージ名が distro で異なる場合は distro/ のマップで変換する。
# ここは常に導入する層。デスクトップとして使う環境だけに要る物は desktop を参照。
# ==================================================

# システム関連
ca-certificates

# セキュリティ関連
nftables        # iptables の後継

# 開発ツール
build-essential # ネイティブビルドツール一式
clang           # Clang コンパイラ
libclang-dev    # libclang 開発ヘッダー
cmake           # CMake
pkg-config      # ネイティブビルドで必要
