# ==================================================
# Linux custom アプリ（サーバのみ）
# ==================================================
# custom.toml と同じ書式だが、表示先の無い Linux（VPS・コンテナ）にだけ適用する。
# ホストの構成は keel が持つので、ここは「ssh して作業する人」のための物だけ。
#
# 現在の宣言は無い。
# ==================================================
