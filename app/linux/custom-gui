# ==================================================
# Linux custom インストーラ（デスクトップ環境のみ）
# ==================================================
# custom と同じ仕組みだが、デスクトップとして使う環境にだけ適用する。
# WSL / headless では実行しない（ターミナルエミュレータ等はホスト側にあるため）。
# ==================================================

# ターミナル
ghostty # Arch・Fedora=PM / Debian 系は公式リポジトリに入るまで community PPA
